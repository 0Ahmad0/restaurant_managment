import 'package:flutter/material.dart';

class ShoppingCartViewBody extends StatelessWidget {
  const ShoppingCartViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
